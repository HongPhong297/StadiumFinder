import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      'plus.unsplash.com',
      'images.unsplash.com',
      'unsplash.com',
      'res.cloudinary.com',
      'i.imgur.com'
    ],
  },
};

export default nextConfig;
